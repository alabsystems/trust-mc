# Experimental Features

We elaborate on some of the more commonly used experimental features in trust-mc.
This is not an exhaustive list; to see all of trust-mc's experimental features, run `targo trust-mc --help`.
To use an experimental feature, invoke trust-mc with the `--unstable` or `-Z` flag followed by the name of the feature.